<% if user_signed_in? %>
	<%= render 'admin/navbars/admin_navbar' %>
<%end%>
<% if user_signed_in? %>
	<%= select_user_create_form(current_user) %>
<%else%>
	<%= render partial: "devise/registrations/new_partials/form" %>
<%end%>
