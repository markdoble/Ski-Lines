<% if user_signed_in? %>
	<%= render 'admin/navbars/admin_navbar' %>
<%end%>
<% if user_signed_in? %>
	<%= select_create_form(current_user) %>
<%else%>
	<%= render partial: "devise/registrations/form" %> 
<%end%>
