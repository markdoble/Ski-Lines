<%= form_for [:admin, @product_category] do |f| %>
  <% if @product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_category.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @product_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<div class="row">
	<div class="col-lg-4 col-md-4 col-sm-6 cl-xs-12">
		  
			<%= f.label :category, "New Category", hidden: true%>
			<%= f.text_field :category, class: "form-control", placeholder: "New Category" %> 
		<br />
		  
	</div>
</div>


<div class="row">
  <div class="col-lg-4 col-md-4 col-sm-6 cl-xs-12 actions">
    <%= f.submit "Save", class: "btn btn-large btn-primary" %>
	<%= link_to 'Cancel', admin_product_categories_path, class: "btn btn-large btn-primary" %>
  </div>
</div>
<% end %>


