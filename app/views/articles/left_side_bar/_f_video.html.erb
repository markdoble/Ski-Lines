	<div class="featured_article_well">
	  
 	 <div class="row">
   	  <div class="featured_photo_well col-lg-12 col-md-12"> 
			
   			<iframe id="player" class="video_well" width="100%" height="300px" src="<%="#{article.image}"%>" frameborder="0" allowfullscreen></iframe>
  			
   	 
	  
 	     	  <div class="col-lg-12 col-md-12 col-sm-12" >
 	     	      <h4 class="featured_article_title">
 			<%= link_to  article.title, article_path(article) %> 
	  			
			 
 	  		  </h4>
		 
 	    		  <p class="article_date">
 	    		<%= Date.parse(article.date_published).strftime("%B %d, %Y") %>
 	    	      </p>
		  
		 
		 
		 	
 	          </div>
			
    	  </div>
	  
   
	  
	  
   	  
  	   </div>
	   
	  
	    
		
		
 	</div>
	
	
	
	 
	
	<script>      // 2. This code loads the IFrame Player API code asynchronously.
	      var tag = document.createElement('script');
	      tag.src = "http://www.youtube.com/player_api";
	      var firstScriptTag = document.getElementsByTagName('script')[0];
	      firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);

	      // 3. This function creates an <iframe> (and YouTube player)
	      //    after the API code downloads.
	      var player;
	      function onYouTubePlayerAPIReady() {
	        player = new YT.Player('player', {
	          playerVars: { 'autoplay': 1, 'controls': 1,'autohide':1,'wmode':'opaque' },
	          videoId: 'JW5meKfy3fY',
	          events: {
	            'onReady': onPlayerReady}
	        });
	      }

	      // 4. The API will call this function when the video player is ready.
	      function onPlayerReady(event) {
	        event.target.mute();
	      }
	</script>
	